for the best application of the ESKF on the IMU(s) we need 


# 1 Data

- high frequency gyroscopic data (uncalibrated)
	about 400 hz minimum (1000 hz is the maximum that can be provided by the BNO085)
	
- medium frequency accelerometer data 
	accelerometer data has Scale factor, Non-orthogonality and bias
	this needs a b (bias vector)
	S  (accouns for Scale factor and Non Orthogonality) for proper calibration of the axis
	
- Low frequency Rotation vector data 
	This acts as the value that stops global drifting of the 

The extremely high requirement of data is bottlenecked by the I2C connection an SPI based connection bypasses this bottle neck

# 2 Calibration and Tuning 

- How to calibrate b and S for linear acceleration
	The standard approach is six-position calibration — hold the IMU stationary in 6 orientations (each face pointing down) and record the mean reading. Since you know the true value is ±9.81 on the relevant axis and 0 on the others, you have enough equations to solve for b and S:

- How to calculate covariance Matrix R in real time
	Options are 
	1) constant R
	2) sliding window
	3)  Initialise with constant R + estimate using innovation while running the ESKF
	I am going ahead with the 3rd option 

	initial R is calculated along with B and s using the calibration script

- Spike detection
	Spikes are not part of gaussian nois that the ESKF is built to overcome
	these need to be removed systematically before providing the data to the filter
	**adaptive** thresholding is ideal for our case with small frequent spikes
	
	Ok, so those werent spikes ![[Pasted image 20260510102530.png]]
	Those are bit flips due to a race condition - we need the Int connected here and perform the reads only after getting interrupts - potentially harming the data collection but hope fully imporving accuracy by upto 8 LSB

# 3 Proposed Design (mini matsya)

## Drivers
- instead of routing like most data have a separate IMU reader which is connected to 1 or 2 IMUs 
- The reader simply uses pyserial to read from the port (that the pico is connected to over USB) and publishes to necessary topics like 
	  /imu1/data/gyro: 199.872     (200)                                                                         
	  /imu1/data/accel: 128.911  (100)                                                                         
	  /imu1/data/rot_vec : 49.991 (50)
	  (the numbers are the rates at which the topic was being published to)
## ESKF based Localisation

- ESKF_localisation.py - Performs ESKF on the given data and publishes to /localisation/pose


# 4 Sensor Fusion

- Complicated - Will think about this later if needed